import { Box } from '@mui/material';
import { FC, memo } from 'react';

const PettyCashSummarySheet: FC<Props> = () => {
  return <Box>test</Box>;
};

type Props = {};

export default memo(PettyCashSummarySheet);
